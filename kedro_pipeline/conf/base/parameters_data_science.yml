model_options:
  # split_data parameters
  test_size: "${runtime_params:test_size, 0.2}"
  random_state: "${runtime_params:random_state, 3}"
  features:
    - Sex
    - Length
    - Diameter
    - Height
    - Weight
    - Shucked Weight
    - Viscera Weight
    - Shell Weight
    - Age
    - Volume
    - Weight proportion
    - Shucked proportion
    - Viscera proportion
    - Shell proportion
    - Shell area
  # create_rf_model parameters
  max_depth: "${runtime_params:max_depth, Null}"
  max_features: "${runtime_params:max_features, 1.0}"
  n_estimators: "${runtime_params:n_estimators, 100}"
  min_samples_leaf: "${runtime_params:min_samples_leaf, 1}"
  min_samples_split: "${runtime_params:min_samples_split, 2}"